import styled from "styled-components";

const S = {
    container: styled.div`
        display: flex;
        justify-content: space-between;
        align-items: stretch;
        flex-wrap: wrap;
        gap: 24px;
        padding: 24px;
    `,
};

export default S;
