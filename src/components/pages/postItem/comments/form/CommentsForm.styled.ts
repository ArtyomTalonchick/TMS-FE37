import styled from "styled-components";
import C from "../../../../../styledComponents";

const S = {
    container: styled.form`
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between; 
    `,
    button: styled(C.button)`
        margin-left: 24px;
    `,
};

export default S;
