import styled from "styled-components";
import { styles } from "../../../globalStyle";

const S = {
    container: styled.div`
        ${styles.card};
        width: 600px;
        margin-top: 24px;
    `,
};

export default S;
